version: "3.7"
services:
  web:
    image: nerevarishe/vista_portal:latest
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - vista_portal:/srv/www/vista_portal/
    restart: always
volumes:
  vista_portal:
